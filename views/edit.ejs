<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h3>edit your chat: <%= chat._id %></h3>
    <p> ths chat was sent from <%= chat.from%> to <%= chat.to %></p>
<form action = "/chats/<%= chat._id%>?_method = PUT" method = "POST">
    <textarea name = "msg" rows = "8" cols = "50"><%= chat.msg %></textarea>
    <br>
    <button>Edit</button>
</form>
</body>
</html>